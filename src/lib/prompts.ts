/**
 * 짱샘 유튜브 메이커 — Claude 프롬프트 템플릿 ("짱샘 두뇌")
 *
 * 기존 jjangsaem-bookshop /generate-youtube-video 스킬의 규칙
 * (Hook 10패턴 · TTS 친화 · CTA · 썸네일 · 디스크립션·태그)을
 * "짱샘 단독 내레이션" 버전으로 재구성.
 *
 * 모든 프롬프트는 결과를 ```json 펜스로 감싼 구조화 출력으로 요청한다.
 * → 자동(API)/수동(Claude.ai 붙여넣기) 모드가 동일한 파서로 흐른다.
 */

import type { ParsedBlog } from "./parser";

/** 짱샘 페르소나·톤 공통 지침 */
export const PERSONA = `너는 "짱샘"이다. 25년차 소아 재활치료사이며 발달지연·이른둥이 아동 재활 전문가다.
대상 시청자는 발달이 걱정되는 아이를 키우는 30~40대 부모다.
톤: 부모에게 옆집 전문가가 차 한잔 하며 들려주듯 친근하면서도 전문적. 따뜻하고 신뢰감 있게, 강의처럼 딱딱하지 않게.
'진료실'이라는 단어는 절대 쓰지 말 것 — 반드시 '치료실'.`;

/** Hook 10패턴 요약 (대본 첫 문장용) */
export const HOOK_PATTERNS = `Hook 패턴 (영상 첫 문장은 반드시 아래 중 하나로 시작):
A 충격적 문제 제기(가정형) — "만약 ~라면 어떨까요?"
B 상식 반박 — "~라고 믿지만 사실은 ~"
C 시간 약속 — "다음 N분만 보시면 ~"
D 통계/충격 수치 — 구체 숫자를 첫 줄에
E 비밀/교과서 밖 — "치료실에서는 잘 말해주지 않는~"
F 시청자 정조준 — "지금 ~하고 계신가요? 그게 ~"
G 회상 자극 — "마지막으로 ~한 게 언제였나요?"
H 결과 먼저 — 핵심 결과를 첫 컷에
I 최근 발견 — "저도 최근에서야 알게 된~"
J 비교 한 줄 — "A와 B, 단 한 가지로 갈립니다"`;

/** TTS 친화 규칙 (단독 내레이션) */
export const TTS_RULES = `대본 작성 규칙 (Google AI Studio TTS 단독 보이스로 녹음):
- 발화 안에 소괄호 ( ) 절대 금지. 영문 약어·부연은 쉼표·줄바꿈으로 풀어 쓴다.
  예) ❌ "ATNR이라는 반사(비대칭 긴장성 목 반사)" → ✅ "ATNR, 비대칭 긴장성 목 반사라고 부르는 반사"
- 마크다운 기호(**, #, > 등) 금지. 순수 텍스트.
- 화자 라벨·연출 메모·[자막] 같은 메타 라인 금지. 내레이션 텍스트만.
- 문장 길이를 의도적으로 변주(짧·짧·긴 리듬). 같은 길이 문장 3연속 금지.
- 도입 금지문구: "안녕하세요", "오늘은 ~에 대해 알아보겠습니다", "이번 영상에서는".
- 어휘: 아이 키우는 부모가 일상에서 실제로 쓰는 쉬운 말을 쓴다. 기계·전자 비유("스위치를 켜고 끄듯", "버튼을 누르듯", "회로", "프로그래밍" 류)는 쓰지 않는다. 특히 '켜고 끄고' 식 표현은 금지. 같은 비유·표현을 반복하지 말고, 꼭 비유가 필요하면 일상 육아 상황에 빗댄다.`;

/** 롱폼/숏폼 구분 */
export type VideoFormat = "long" | "short";

/** 영상 구조·리텐션 규칙 (2026 발달/육아 채널 기준) */
export const SCRIPT_STRUCTURE = `영상 구조·이탈 방지 규칙 (육아·치료에 지친 시청자 기준):
- 분량: 공백 제외 한국어 발화 기준 분당 약 300~350자. (예: 8분 영상 ≈ 2,400~2,800자)
- [인트로 훅 / 0~30초] 인사·채널 소개는 생략하거나 뒤로 미룬다. 시작하자마자 부모·치료사가 겪는 가장 가려운 통점을 짚고, 이 영상으로 얻을 '하나의 명확한 해결책'을 약속한다. 두괄식.
  예) "STNR, 대칭성 긴장성 목 반사가 안 빠지면 아이가 똑바로 앉지 못합니다. 오늘 집에서 3분 만에 체크하는 법을 알려드릴게요."
- [본론 / 30초~7분] 핵심 요점을 3가지 이내로 쪼갠다. 전문 용어(TLR, ATNR 등)가 나오면 반드시 그 자리에서 쉬운 말로 풀고, 해당 슬라이드(시각자료)가 그 설명을 받친다고 전제하고 쓴다. 매 20~30초마다 화면 전환·예시·반전 등 '패턴 브레이크'가 들어갈 지점을 의식해 리듬을 끊어준다.
- [아웃트로 / 7~8분] 핵심을 요약한 뒤, 무거운 구독 요청 대신 "이 원리를 알았다면 다음은 이 영상을 보세요"로 다음 단계 영상(엔드스크린)으로 연결해 연쇄 시청을 유도한다.`;

/** 숏폼(YouTube Shorts/릴스) 구조 규칙 — 60초 미만 */
export const SHORTS_STRUCTURE = `숏폼(YouTube Shorts) 구조 규칙 (60초 미만, 짧고 빠른 호흡):
- 분량: 공백 제외 한국어 발화 기준 총 250~320자(약 45~55초). 절대 60초를 넘기지 말 것.
- 컷 수: 훅 1개 + 본론 4~6개 + 마무리 1개 = 총 6~8컷. 컷 하나하나가 화면이 바뀌는 '비주얼 비트'다. 컷이 2~3개뿐이면 화면이 정지된 느낌이라 실패. 빠르게 갈아끼운다.
- 모든 컷은 반드시 화면(이미지)을 가진다. 내레이션만 있고 화면이 빈 컷은 만들지 않는다.
- [0~3초 훅] 첫 문장에서 즉시 가장 센 통점·반전을 던져 스크롤을 멈추게 한다. 인사·채널 소개·도입부 일절 금지.
- [본론] 메시지는 '핵심 하나'만(주제는 하나로 좁히되 그 하나를 여러 컷으로 잘게 쪼개 빠른 템포로 보여준다). 곁가지·다른 주제 나열 금지. 짧게 끊어지는 문장. 전문 용어가 꼭 필요하면 한 단어로만 풀고 바로 넘어간다.
- [마무리 3~5초] 맨 처음 잡은 '핵심 정보(진짜 해법)'를 한 번 더 또렷이 남기며 끝낸다. 부모가 무엇을 기억하고 무엇을 해보면 되는지가 분명해야 한다. 따뜻하게 닫되, 곁가지(순서·루틴 등)나 내용 없는 위로·격언으로 흐리지 말 것. '구독'·'좋아요'·'팔로우' 같은 채널 행동 유도(유튜브에 '팔로우'는 없다), '저장하세요' 식 저장 유도, '풀영상/자세한 건 영상에서' 식 다른 영상 유도, 정보 제공·면책 안내는 대본에 넣지 않는다.
- 단정적 인과("무조건 좋아집니다") 금지. 짧아도 개별 차이는 한 번 짚는다.`;

/** 참고 — 조회수가 잘 나온 발달/육아 전문가 영상 대본의 결 (롱폼·숏폼 공통) */
export const REFERENCE_STYLE = `참고 — 조회수가 잘 나온 발달·육아 전문가 영상 대본의 결(이 톤을 따른다):
- 사족 없이 본론부터. 한 문장 한 문장이 곧 정보가 되게(빈말·추임새·인사 최소화). 첫 문장부터 핵심을 짚는다.
- 모호한 말 대신 '구체적인 기준'을 준다. 예) "18개월에도 혼자 걷지 못하면", "이름을 불러도 반응이 없거나 원하는 걸 손가락으로 가리키지 못하면" 처럼 나이·행동으로 딱 짚어 부모가 자기 아이에 바로 대입할 수 있게 한다.
- 부모가 당장 해볼 수 있는 것을 알려준다. 집에서 관찰할 신호, 어디서 확인·검사할 수 있는지(기관·자료) 같은 실질적 정보를 담는다.
- 차분하지만 전문가의 확신이 느껴지게. "원인을 빨리 찾고 빨리 도울수록 결과가 좋아진다"처럼 조기 개입의 희망을 전한다.
- 과장·공포 조장 금지. 마지막은 부모를 다그치는 행동 유도가 아니라, 힘이 되는 한마디로 닫는다(예: "양육 환경을 바꾸는 것만으로도 좋아지는 아이가 많습니다").`;

/**
 * 짱샘 말맛(자연스러움) 교정 노트.
 * 사장님이 직접 손본 대본에서 반복적으로 나타나는 '결'을 누적한다.
 * 새로운 수정본을 받을 때마다 여기에 한 줄씩 더한다(원칙은 그대로, 사례만 늘린다).
 * 핵심 한 줄: "단정하지 말고, 옆에서 설명하듯 풀고, 여지를 둔다."
 */
export const NATURAL_TONE = `[짱샘 말맛 — 자연스러움 교정 (대본을 더 사람 말처럼)]
딱 떨어지는 '문어체 단정'을 피하고, 부모 옆에서 차 한잔 하며 설명하듯 풀어 쓴다. 아래는 실제 교정에서 반복된 결이다.
1) 종결어미를 설명·대화체로: "~예요/~있어요" 같은 평평한 단정 대신 "~여서 그래요 / ~거든요 / ~인 거예요"처럼 풀어주는 어미를 섞는다. (단, 매 문장 같은 어미 반복은 금지 — 단정·설명·질문 어미를 섞어 리듬을 준다.)
2) 도입 훅은 질문으로 공감 유도: 첫 문장을 마침표로 닫지 말고 "~죠?"처럼 물어, 부모가 "맞아, 우리 애가 그래" 하고 끄덕이게 한다. (예: "백 번 야단쳐도 안 고쳐지죠?")
3) 단정을 완화해 여지를 둔다: "자리잡아요→자리잡기 시작해요", "볼 수 있어요→볼 수도 있습니다", "흔해요→흔하게 보이는 현상이에요"처럼 '시작/도/수도/현상'을 넣어 100% 단언을 피한다. 아이마다 다름을 어미에서부터 인정한다.
4) 명령형을 연결형으로 부드럽게: 조언을 "~하세요"로 툭 끊지 말고 "~해 주시고요 / ~해 주세요"처럼 다음 문장으로 이어지게 한다.
5) 독자의 속마음을 단정하지 않는다: "~라고 믿으시죠"처럼 부모의 생각을 못박지 말고 "~라고 생각하실 수 있는데요"로 여지를 둔다(공감하되 가르치려 들지 않게).
6) 어휘 선택: 아이를 나무라는 맥락은 '혼내다'보다 '야단치다'를 쓰고, 한 대본 안에서 같은 말로 일관한다.
7) 부사·형용사로 톤을 살짝 보강: "긴장하고→더 긴장하고", "풀리는 신호→풀리는 자연스러운 신호"처럼 한 단어로 결을 따뜻하고 또렷하게.
8) 마무리 위로는 간결한 '허락형'으로: "천천히요" 같은 토막말보다 "천천히 해도 됩니다"처럼 부모의 마음을 놓아주는 완결된 한마디로 닫는다.
이 결은 '말맛'을 위한 것이지 정보를 흐리려는 게 아니다. 핵심 정보 전달과 논리 연결을 해치지 않는 선에서 적용한다.`;

/**
 * 가장 먼저 — 입력 .txt의 '핵심 정보 한 가지'를 정확히 잡고 거기에 대본을 집중시킨다.
 * 보조 설명(호르몬 등)이나 곁가지 일반론(순서·루틴)을 핵심으로 착각하지 않게 한다. (롱폼·숏폼 공통)
 */
export const CORE_MESSAGE_FIRST = `[가장 먼저 — 이 글의 '핵심 정보' 한 가지를 정확히 잡는다]
대본을 쓰기 전에 입력 .txt(본문·메타·슬라이드)를 끝까지 읽고, "이 글이 부모에게 진짜로 전하려는 가장 중요한 정보 한 가지"가 무엇인지 먼저 한 문장으로 못박는다. 그 한 문장이 이 대본의 목적지다.
- 핵심은 보통 '문제의 진짜 원인'과 '그래서 무엇을, 어떻게 하면 되는지(핵심 해법·작동 원리)'이다. 글이 여러 보조 설명을 동원하는 건(예: 코르티솔·멜라토닌 같은 호르몬 이야기) 결국 이 핵심 한 가지를 납득시키기 위한 '근거'일 뿐이다. 보조 설명을 핵심으로 착각하지 말 것.
- 곁가지·일반론(예: "순서를 지켜라", "루틴을 만들어라", "잘 안아줘라" 같은 두루뭉술한 조언)을 핵심 자리에 올리지 않는다. 글 전체가 가리키는 '진짜 해법'을 핵심으로 삼는다. (예: 글이 호흡과 향기로 아이의 신경계를 안정시키는 법을 말한다면, 핵심은 '호흡·후각으로 신경계를 긴장에서 회복으로 돌린다'이지 '순서 지키기'가 아니다.)
- 대본의 모든 컷, 특히 마지막 컷은 이 핵심 정보를 분명하고 정확하게 '전달'하는 데 복무한다. 마지막을 내용 없는 위로 한마디로 흐리지 말고, 핵심 해법을 한 번 더 또렷이 남긴다(부모가 무엇을 기억하고 무엇을 해보면 되는지).`;

/**
 * 출력 전 자가 검토 — 대본을 다 쓴 뒤 문맥/논리/정확성/목적부합을 스스로 점검하고
 * 고친 '최종본'만 내보내게 한다. (롱폼·숏폼 공통)
 */
export const FINAL_REVIEW = `[대본 완성 후 — 출력 전 자가 검토 (필수)]
대본 초안을 다 쓴 뒤, 곧바로 내보내지 말고 아래를 스스로 점검한다. 어긋난 곳을 고쳐서 '최종본'만 출력한다(초안·검토 메모·점검 과정은 절대 출력하지 말 것).
1) 문맥·논리 연결 (가장 중요 — 가장 자주 무너지는 지점):
   대본을 '인접한 두 문장'씩 짝지어 처음부터 끝까지 훑는다. 각 짝마다 "뒷 문장이 앞 문장에 정말로 이어지는가? 같은 주제, 같은 축(원인↔원인, 방법↔방법, 믿음↔그 믿음에 대한 반박) 위에서 말하고 있는가?"를 묻는다. 한 군데라도 "어?" 하고 걸리면 반드시 고친다.
   특히 접속·대조 표현은 '약속'이다. 그 약속을 지키지 못할 표현은 쓰지 않는다.
   · "그런데 / 하지만" 으로 뒤집을 때 — 앞 문장이 세운 '바로 그 믿음·기대'를 정면으로 뒤집어야 한다. 앞에서 세운 것과 다른 걸 뒤집으면 안 된다.
   · "A가 아니라 B예요" 로 부정할 때 — 부정 대상 A는 듣는 사람이 '직전 문장에서 떠올렸을 바로 그것'이어야 한다. 앞에서 깔아두지도 않은 A를 갑자기 부정하면 비약이다. A와 B는 같은 종류여야 한다(둘 다 원인, 또는 둘 다 방법).
   · "그래서 / 왜냐하면 / 그러니까" 로 이을 때 — 인과가 실제로 성립해야 한다.
   · 나쁜 예 1) "낮에 더 뛰어놀게 하면 밤에 잘 잔다고 믿으시죠? 그런데 새벽 4시에 깨는 아이는 지쳐서가 아니라, 신경계가 긴장을 못 풀어서예요." → 앞에서 세운 믿음은 '많이 놀면 잘 잔다'인데, 뒷 문장은 '지쳐서가 아니라'를 부정한다. 부정 대상(지쳐서)이 앞에서 또렷이 깔리지 않아 축이 어긋나고, '믿으시죠?'에 대한 반박으로도 빗나간다.
     · 고친 예) "낮에 실컷 뛰어놀면 밤엔 푹 잘 거라 믿으시죠? 그런데 종일 신나게 논 날도 새벽 4시에 깨는 아이가 있어요. 몸이 지치는 것과 깊이 잠드는 건 다른 문제거든요." (믿음 → 그 믿음이 빗나가는 사례 → 이유, 로 축이 한 줄로 이어짐.)
   · 나쁜 예 2) "긴장을 풀어주는 가장 짧은 길은 호흡과 향기예요." 다음에 "안전감 없이 호흡부터 시키면 더 긴장해요." → 앞에서 '호흡부터 시키는 상황'을 꺼낸 적이 없는데 갑자기 경고가 나와 비약.
     · 고치는 법) 연결 다리를 놓거나(예: "단, 순서가 있어요. 안전감이 먼저예요."), 순서를 바꾸거나, 한쪽을 덜어낸다.
   이렇게 인접 문장의 축을 모두 맞춰 비약·모순이 0이 될 때까지 다듬는다.
2) 용어·개념 일관성: 같은 개념을 가리키는 말이 컷마다 흔들리지 않게 한다. 한 번 쉽게 풀어준 용어는 그 뒤로도 같은 말로 받는다.
3) 정보의 정확성·근거: 입력으로 받은 .txt(블로그 본문·메타)에 실제로 근거가 있는 내용인지 다시 대조한다. 본문에 없는 수치·사실을 지어내지 않는다. 모호한 위로성 문장보다 구체적이고 실행 가능한 정보를 우선한다.
4) 핵심 정보 전달 자가 평가 (이 채널은 발달장애·발달지연 아이를 키우는 부모에게 '정말 유용한 정보'를 주는 곳이다):
   · 맨 처음 한 문장으로 못박은 '핵심 정보'가, 대본 전체에 또렷이 살아 있는가? 특히 마지막 컷이 그 핵심 해법을 분명히 '전달'하며 끝나는가?
   · 마지막이 핵심 대신 곁가지(순서·루틴 등 일반론)나 내용 없는 위로·격언으로 흐려지지 않았는가? 그렇다면 마지막 컷을 핵심 정보로 다시 채운다.
   · 핵심이 보조 설명(호르몬 이야기 등)에 묻혀 정작 '그래서 무엇을 하면 되는지'가 빠지지 않았는가?
   · 부족하면 정보 밀도를 끌어올리고 핵심을 전면에 세운 뒤 확정한다.
위 1~4를 모두 통과시킨 최종 대본만 아래 JSON으로 출력한다.`;

function blogContext(blog: ParsedBlog): string {
  // 각 슬라이드가 '실제로 화면에 그리는 것'(영문 프롬프트)을 그대로 노출한다.
  // 대본은 이 시각 내용을 보고 그것을 짚어 설명하는 내레이션을 써야 한다.
  const slotLines = blog.imageSlots
    .map(
      (s) =>
        `── 슬라이드 #${s.index} [${s.type}]${s.isHero ? " (대표/썸네일 후보)" : ""}
이 슬라이드가 화면에 보여주는 것(영문 시각 설계):
${s.prompt || "(시각 설명 없음)"}`
    )
    .join("\n\n");
  return `[원본 블로그 메타]
제목: ${blog.meta.title ?? ""}
카테고리: ${blog.meta.category ?? ""}
메인 키워드: ${blog.meta.mainKeyword ?? ""}
서브 키워드: ${blog.meta.subKeywords.join(", ")}
롱테일 키워드: ${blog.meta.longtailKeywords.join(", ")}
관련 전자책: ${blog.meta.relatedEbook ?? ""}

[인포그래픽 슬라이드 ${blog.imageSlots.length}장 — 영상의 시각 트랙]
아래는 영상에서 순서대로 화면에 뜨는 인포그래픽 슬라이드다. 각 슬라이드의 '화면에 보여주는 것'은
이미 확정된 시각 자료이며, 대본은 이 그림을 바꾸는 게 아니라 이 그림을 보면서 강의하듯 풀어 설명한다.
imageRef=N 인 내레이션은 반드시 #N 슬라이드에 실제로 그려진 위 내용과 일치해야 한다
(슬라이드엔 없는 걸 "보세요"라고 하거나, 슬라이드가 보여주는 핵심을 빠뜨리지 말 것).

${slotLines}

[원본 블로그 본문]
${blog.bodyText}`;
}

/* ────────────────────────────────────────────────────────────────
 * 1) 대본 (짱샘 단독 내레이션) — 주제·앵글·Hook 선택까지 포함
 * ──────────────────────────────────────────────────────────────── */

export interface ScriptSlide {
  /** 매칭되는 인포그래픽 슬라이드 번호. 인트로/아웃트로는 null */
  imageRef: number | null;
  heading: string;
  seconds: number;
  narration: string;
  /**
   * 숏폼 전용: 이 컷 화면을 그릴 1:1 인포그래픽 영문 프롬프트.
   * 블로그 슬라이드를 재활용하는 컷(imageRef 있음)은 비워둘 수 있고,
   * 훅·마무리처럼 대응 슬라이드가 없는 컷은 반드시 채운다.
   */
  imagePrompt?: string;
}

export interface ScriptResult {
  videoTitleWorking: string;
  hookPattern: string; // A~J
  hookPatternLabel: string;
  estimatedMinutes: number;
  slides: ScriptSlide[];
}

export function buildScriptPrompt(
  blog: ParsedBlog,
  opts: { format: VideoFormat; minutes: number }
): string {
  if (opts.format === "short") return buildShortsScriptPrompt(blog);
  return `${PERSONA}

${HOOK_PATTERNS}

${TTS_RULES}

${SCRIPT_STRUCTURE}

${REFERENCE_STYLE}

${NATURAL_TONE}

${CORE_MESSAGE_FIRST}

[작업]
아래 네이버 블로그 글 한 편을, 짱샘이 혼자 내레이션하는 유튜브 영상 대본으로 변환한다.
영상의 시각 트랙은 아래 [인포그래픽 슬라이드]에 '화면에 보여주는 것'까지 명시된 ${blog.imageSlots.length}장이다.
각 슬라이드가 화면에 떠 있는 동안 짱샘이 그 슬라이드에 실제로 그려진 것을 짚으며 풀어 설명하는 구조로, 슬라이드 순서대로 내레이션을 배치한다.
- 맨 앞에 imageRef=null 인 인트로(Hook) 1개를 둔다. 영상 첫 문장은 Hook 패턴 A~J 중 가장 맞는 것으로 시작.
- 각 인포그래픽 슬라이드(#1~#${blog.imageSlots.length})마다 슬라이드 1개를 만들고 imageRef에 번호를 넣는다. 그 narration 은 #N 슬라이드의 '화면에 보여주는 것'에 그려진 요소를 실제로 가리키며 설명해야 하고(슬라이드에 없는 장면을 지어내지 말 것), 본문은 그 시각 자료를 말로 풀기 위한 근거로만 쓴다.
- 3~4번째 슬라이드 직전 / 중반(50%) / 마무리 직전에 retention hook 한 줄("그런데 진짜 중요한 건", "여기서 많은 분이 놓치는 게" 등)을 내레이션에 자연스럽게 녹인다.
- 맨 끝에 imageRef=null 인 아웃트로 1개: 맨 처음 잡은 '핵심 정보(진짜 해법)'를 정확히 담은 3줄 요약(곁가지·일반론이 아니라 그 핵심을 부모가 무엇을 어떻게 하면 되는지로 또렷이) + "이 원리를 알았다면 다음은 ~ 영상을 보세요" 식 다음 단계 영상 유도(연쇄 시청) + 짱샘의 책방「${blog.meta.title ?? "관련 전자책"}」 안내 + YMYL 면책 1줄. 구독·좋아요는 가볍게 한 번만(무겁게 매달리지 말 것). 매번 다른 표현으로.
- 전체 분량 목표: 약 ${opts.minutes}분 (공백 제외 한국어 발화 기준 분당 약 300~350자 = 약 ${opts.minutes * 300}~${opts.minutes * 350}자).
- 본문 내용을 그대로 베끼지 말고 말로 설명하듯 자연스럽게 재구성. 단정적 인과("무조건 좋아집니다") 금지, 개별 차이 명시.

${FINAL_REVIEW}

[출력 — 반드시 아래 JSON 한 덩어리만, 코드펜스로 감싸서]
\`\`\`json
{
  "videoTitleWorking": "영상 내부용 작업 제목",
  "hookPattern": "A~J 중 한 글자",
  "hookPatternLabel": "패턴 한국어 라벨",
  "estimatedMinutes": ${opts.minutes},
  "slides": [
    { "imageRef": null, "heading": "인트로(Hook)", "seconds": 10, "narration": "..." },
    { "imageRef": 1, "heading": "...", "seconds": 45, "narration": "..." }
  ]
}
\`\`\`

${blogContext(blog)}`;
}

/** 숏폼(60초 미만) 대본 — 핵심 하나만 압축 */
export function buildShortsScriptPrompt(blog: ParsedBlog): string {
  return `${PERSONA}

${HOOK_PATTERNS}

${TTS_RULES}

${SHORTS_STRUCTURE}

${REFERENCE_STYLE}

${NATURAL_TONE}

${CORE_MESSAGE_FIRST}

[작업]
아래 네이버 블로그 글 한 편에서 '가장 임팩트 있는 핵심 하나'만 뽑아, 짱샘이 혼자 내레이션하는 60초 미만 유튜브 숏폼 대본으로 압축한다.
컷 구성: 훅 1개 + 본론 4~6개 + 마무리 1개 = 총 6~8컷. 컷마다 화면이 바뀌어야 하므로, '핵심 하나'를 여러 컷으로 잘게 쪼개 빠른 템포로 보여준다.
- 맨 앞 imageRef=null 인 훅 1개: 0~3초, 스크롤을 멈추게 하는 강한 첫 문장(Hook 패턴 A~J 중 가장 센 것).
- 본론 4~6컷: 핵심 하나를 단계적으로 풀어가는 컷들. 가능하면 본문 인포그래픽 슬라이드(#1~#${blog.imageSlots.length})를 재활용해 imageRef 에 그 번호를 넣는다(해당 narration 은 그 슬라이드에 실제로 그려진 것을 짚어 설명). 대응되는 슬라이드가 없으면 imageRef=null 로 두고 imagePrompt 를 직접 채운다. 각 narration 은 1~2문장으로 짧게.
- 맨 끝 imageRef=null 인 마무리 1개: 맨 처음 잡은 '핵심 정보(진짜 해법)'를 한 번 더 또렷이 남기며 끝낸다. 부모가 무엇을 기억하고 무엇을 해보면 되는지가 분명해야 한다. 따뜻하게 닫되, 곁가지(순서·루틴 등)나 내용 없는 위로·격언으로 흐리지 말 것. '구독'·'좋아요'·'팔로우' 같은 채널 행동 유도(유튜브에 '팔로우'는 없다), '저장하세요' 식 저장 유도, '풀영상' 식 다른 영상 유도, 정보 제공·면책 안내는 넣지 않는다.
- ★ 모든 컷은 반드시 화면 이미지를 가진다. imageRef 로 블로그 슬라이드를 재활용하지 않는 컷(훅·마무리·신규 본론 컷)은 imagePrompt 를 반드시 채운다.
  · imagePrompt 작성 규칙: 이 컷의 화면을 그리는 '영문' 프롬프트. 본문 인포그래픽 슬라이드와 같은 화풍(Korean parenting magazine style educational infographic, soft flat shapes, clean line art, calm muted palette, Korean labels in Noto Sans Korean), 단 세로 숏폼용 1:1 1080x1080 square 비율로 명시한다. 이미지 안 텍스트는 짧고 정확한 한국어 한두 단어만. 끝에 "Avoid: photorealistic, 3D render, watermark, distorted Korean text" 류의 네거티브 한 줄.
- 전체 발화 분량: 공백 제외 한국어 기준 총 250~320자(약 45~55초). 절대 초과 금지.
- 본문을 그대로 베끼지 말고 말로 설명하듯 재구성. 단정적 인과 금지.
- 숏폼은 짧아서 비약이 더 잘 보인다. 컷과 컷 사이가 툭툭 끊기지 않게, 앞 컷의 끝과 뒤 컷의 시작이 한 호흡으로 이어지는지 특히 신경 쓴다.

${FINAL_REVIEW}

[출력 — 반드시 아래 JSON 한 덩어리만, 코드펜스로 감싸서]
\`\`\`json
{
  "videoTitleWorking": "영상 내부용 작업 제목",
  "hookPattern": "A~J 중 한 글자",
  "hookPatternLabel": "패턴 한국어 라벨",
  "estimatedMinutes": 1,
  "slides": [
    { "imageRef": null, "heading": "훅", "seconds": 3, "narration": "...", "imagePrompt": "Educational infographic ... 1:1 1080x1080 square ... Avoid: ..." },
    { "imageRef": 2, "heading": "...", "seconds": 9, "narration": "..." },
    { "imageRef": null, "heading": "...", "seconds": 9, "narration": "...", "imagePrompt": "Educational infographic ... 1:1 1080x1080 square ... Avoid: ..." },
    { "imageRef": null, "heading": "마무리", "seconds": 5, "narration": "...", "imagePrompt": "Educational infographic ... 1:1 1080x1080 square ... Avoid: ..." }
  ]
}
\`\`\`

${blogContext(blog)}`;
}

/* ────────────────────────────────────────────────────────────────
 * 2) 메타데이터 (제목 후보 · 디스크립션 · 태그)
 * ──────────────────────────────────────────────────────────────── */

export interface MetadataResult {
  titles: string[]; // 5개
  description: string;
  tags: string[];
  pinnedComment?: string;
}

export function buildMetadataPrompt(blog: ParsedBlog): string {
  // 고정댓글·디스크립션에 넣을 책 링크.
  // txt의 '관련 전자책'에 특정 책 URL이 있으면 그걸, 없으면 책방 메인 URL.
  const bookUrl =
    blog.meta.relatedEbook && /^https?:\/\//.test(blog.meta.relatedEbook.trim())
      ? blog.meta.relatedEbook.trim()
      : "https://jjangsaem.com/n";
  return `${PERSONA}

[작업]
아래 블로그 기반 유튜브 영상의 메타데이터를 작성한다.

요구사항:
- 제목 후보 5개 (SEO + 호기심 동시 충족):
  · 첫 40글자 이내에 핵심 검색어 "${blog.meta.mainKeyword ?? ""}"를 넣되, 뒷부분에서 궁금증을 유발하는 구조.
  · '정보'와 '결과'의 비대칭성: 무엇에 관한 이야기인지는 밝히되 그 '결과·방법'은 비밀로 남겨 클릭하게 만든다.
  · '부정형 프레이밍/손실 회피'를 우선: 부모·치료사는 '더 좋은 것'보다 '놓쳐서 아이에게 해가 되는 것'에 훨씬 민감하다.
  · 5개를 서로 다른 구조로: 호기심형 / 손실회피형 / 타겟팅형 / 숫자형 / 결과비밀형.
    예) 호기심형 "산만한 아이, ADHD가 아니라 '이것'이 원인일 수 있습니다 (STNR 체크법)"
        손실회피형 "발달 치료 수업 전, 반드시 확인해야 할 원시반사 3가지"
        타겟팅형 "자폐 스펙트럼 아동을 둔 부모가 집에서 매일 해야 하는 '이 행동'"
  · 과한 어그로·단정("무조건 낫는다") 금지.
- 디스크립션 (AI가 맥락을 이해해 관련영상 추천에 쓰는 핵심 소스. 타임라인은 넣지 않는다):
  · 첫 2줄: '더보기' 전 노출 구역. 핵심 키워드("${blog.meta.mainKeyword ?? ""}" 등)를 자연스럽게 녹인 핵심 요약 2줄.
  · 본문은 이모지로 문단을 나눠 스캐닝하기 좋게. 예시 문단:
      💡 영상 요약: 이 영상에서 다루는 핵심 한두 문장.
      🌱 이런 분들께 추천해요: 가정에서 아이 발달을 돕고 싶은 부모님, 현장의 아동 치료사 선생님.
      📚 짱샘의 책방「${blog.meta.title ?? ""}」 안내 1줄 (관련 링크: ${bookUrl}).
      ⚠️ 이 영상은 정보 제공을 위한 것이며, 전문의의 진단·상담을 대체하지 않습니다. 아이의 상태가 걱정된다면 가까운 전문기관과 상담하세요. (대본에는 넣지 않고 디스크립션에만 1회 표기)
  · 맨 아래에 해시태그 3~5개만. 대분류→중분류→소분류 순(예: #발달장애아동 #원시반사 #ATNR). 그 이상은 스팸 처리되니 금지.
  · 마크다운 기호 금지, 자연스러운 한국어.
- 태그: 10~15개 이내. 아래 카테고리를 섞는다.
  · 핵심 타겟 키워드 1~3개 (영상 메인 주제)
  · 연관·확장 키워드 3~5개 (검색할 만한 문장형/단어형)
  · 오타·유사어·약어 2~3개 (자주 틀리는 표기, 영문/한글 표기 변형)
  · 채널 고유 태그 1개 (브랜드명)
- 고정댓글 후보 1개: 시청자 참여 유도 질문형. 단, 아래 규칙을 반드시 지킨다.
  · 맨 앞(첫 줄 또는 첫 문장)에 이 영상과 관련된 책 링크를 자연스럽게 안내한다: ${bookUrl}
    영상 내용을 더 깊이 보고 싶은 사람이 바로 갈 수 있게, 댓글 도입에 책방/책 링크를 먼저 노출한다.
    예) "오늘 다룬 6주 순서를 책으로 더 자세히 보고 싶다면 여기에서 확인하세요. ${bookUrl}"
  · 그다음 댓글 참여를 부르는 질문 1~2개를 잇는다(아이의 상황을 묻는 식).
  · '—', '–', '―' 같은 줄표(엠대시/엔대시)를 절대 쓰지 말 것. 끊어 읽기는 쉼표·마침표·줄바꿈으로만 처리한다.

[출력 — JSON 한 덩어리만, 코드펜스로 감싸서]
\`\`\`json
{
  "titles": ["", "", "", "", ""],
  "description": "여러 줄 문자열",
  "tags": ["", ""],
  "pinnedComment": ""
}
\`\`\`

${blogContext(blog)}`;
}

/* ────────────────────────────────────────────────────────────────
 * 3) 썸네일 (이미지 프롬프트 + 문구 5개)
 * ──────────────────────────────────────────────────────────────── */

export interface ThumbnailResult {
  imagePrompt: string;
  negativePrompt: string;
  phrases: string[]; // 5개, 8~12자(3~4단어)
  miniCopyLeft: string;
  miniCopyRight: string;
}

export function buildThumbnailPrompt(blog: ParsedBlog): string {
  const hero = blog.imageSlots.find((s) => s.isHero) ?? blog.imageSlots[0];
  return `${PERSONA}

[작업]
아래 블로그 기반 유튜브 영상의 썸네일을 설계한다.
썸네일은 본문 슬라이드(카툰 인포그래픽)와 달리, 피드에서 클릭을 부르는 '실사풍 고품질 이미지'로 만든다. 납작한 일러스트가 아니라 디테일과 깊이가 있는 사실적인 장면으로, 영상 주제를 한눈에 전달한다.

[썸네일 이미지 생성 규칙 — 반드시 지킬 것]
1. 화풍: photorealistic(실사). 따뜻하고 영화 같은(cinematic) 자연광, 부드러운 실내 분위기. 한국 육아·발달 채널에 어울리는 사실적인 한국 아기/아이와 실제 같은 피부·옷감 질감. 납작한 플랫 일러스트·만화체·장난감 3D 렌더는 금지.
2. 풍부한 장면(너무 단순 금지): 인물 뒤에 맥락이 느껴지는 환경을 얕은 심도(shallow depth of field, 배경 보케)로 깔아 깊이를 준다 — 예: 포근한 가정 거실, 햇살 드는 놀이방, 밝고 깨끗한 치료실 한켠. 상황을 암시하는 소품 1~2개는 OK, 단 산만하지 않게.
3. 피사체는 크게 (가장 중요): 영상 핵심을 보여주는 한국 아이 '한 명'을 close-up~medium close-up 으로 크게. 인물이 프레임 세로의 약 60~75% 를 채운다. 넓은 배경에 작게 떠 있게(tiny figure, full-body wide shot) 금지. 표정·동작은 영상 주제와 직결되게(예: 까치발, 놀란 표정, 특정 앉은 자세) 하되 과장·공포는 지양하고 진정성 있게.
4. 치우친 구도 + 제목 공간: 아이를 한쪽(예: 오른쪽 약 55~60% 폭)에 크게 배치하고, 반대쪽 약 40% 는 제목 얹을 공간으로 비운다 — 단 텅 빈 게 아니라 흐린 배경·은은한 빛으로 자연스럽게 채워 허전하지 않게. 위아래 중앙 띄우기(centered floating) 금지.
5. 시선 유도·색감: 핵심 인물에 부드러운 림라이트/하이라이트로 시선을 모은다. 따뜻한 톤, 부드럽지만 고대비, 채도 약간 높게(피드에서 눈에 띄게).
6. 텍스트 절대 금지: 이미지 안에 한글·영문·숫자·로고·워터마크를 일절 넣지 않는다(문구는 편집 단계에서 합성).
7. 비율: 16:9, 1920x1080.

- imagePrompt: 위 1~7 을 모두 반영한 '영문' 프롬프트(여러 줄). photorealistic, cinematic warm lighting, shallow depth of field, detailed, large subject filling most of the frame, placed on the right side 같은 사실성·디테일·크기·구도 단어를 명시. 영상 주제의 핵심 행동/상황을 사실적 장면으로 묘사. 끝에 네거티브를 한 줄로 붙인다.
- negativePrompt: 영문. 최소한 아래를 포함한다: "no text, no captions, no letters, no Korean characters, no numbers, no logos, no watermark, flat cartoon illustration, anime, plastic 3D toy render, extra fingers, deformed hands, distorted face, multiple identical children, busy cluttered background, tiny figure, subject floating in empty background, full-body wide shot with large empty margins, exaggerated scary expression, low quality, blurry".
- phrases: 썸네일에 얹을 한국어 '메인 문구' 후보 5개 (이 중 하나를 골라 쓰는 용도, 나머지는 A/B 테스트 예비). 제목의 요약이 아니라 감정·호기심·즉각적 이득을 때리는 카피.
  · 길이·형식: 너무 짧아 밋밋하지 않게, 한 줄이면 12~18자, 또는 '큰 줄 + 받는 줄' 두 줄 구조(각 줄 8~14자)로. 두 줄일 때는 줄바꿈을 \\n 으로 표시. 핵심 단어가 한눈에 크게 읽히도록 군더더기 조사·접속어는 빼고 압축.
  · 제목에 이미 있는 단어를 그대로 반복하지 말고 제목과 '보완' 관계로. 과한 어그로 금지(예: "우리 아이 자폐일까?"처럼 거부감 유발하는 직접 단정 금지). 5개를 서로 다른 패턴(호기심형/숫자형/질문형/반전형/결과형)으로. 느낌표·물음표는 후보당 최대 2개.
    좋은 예) "잘 놀라는 아이\\n혹시 이 반사 때문?" / "밤마다 깨는 진짜 이유" / "치료실에선 안 알려주는 3가지" / "이 자세, 그냥 두면 안 됩니다"
- miniCopyLeft: 좌측 상단 8자 이내 후크(예: "혹시 우리 아이도?").
- miniCopyRight: "25년차 소아발달 재활 전문가" 고정.

[원본 대표 인포그래픽 프롬프트 — '무엇을 보여줄지' 소재만 참고(카툰 화풍은 따르지 말 것, 같은 주제를 실사 장면으로 옮긴다)]
${hero?.prompt ?? "(없음)"}

[출력 — JSON 한 덩어리만, 코드펜스로 감싸서]
\`\`\`json
{
  "imagePrompt": "",
  "negativePrompt": "",
  "phrases": ["", "", "", "", ""],
  "miniCopyLeft": "",
  "miniCopyRight": "25년차 소아발달 재활 전문가"
}
\`\`\`

제목: ${blog.meta.title ?? ""}
메인 키워드: ${blog.meta.mainKeyword ?? ""}`;
}

/* ────────────────────────────────────────────────────────────────
 * 공통 — Claude 응답에서 JSON 추출
 * ──────────────────────────────────────────────────────────────── */

export function extractJson<T>(text: string): T {
  // ```json ... ``` 우선, 없으면 첫 { ~ 마지막 }
  const fence = text.match(/```(?:json)?\s*([\s\S]*?)```/);
  const candidate = fence ? fence[1] : text;
  const start = candidate.indexOf("{");
  const end = candidate.lastIndexOf("}");
  if (start === -1 || end === -1) {
    throw new Error("JSON을 찾지 못했습니다. Claude 응답 형식을 확인하세요.");
  }
  return JSON.parse(candidate.slice(start, end + 1)) as T;
}

/* ────────────────────────────────────────────────────────────────
 * 이미지 프롬프트 — 블로그용 → 유튜브 슬라이드용 보정 (코드 레벨, Claude 불필요)
 * ──────────────────────────────────────────────────────────────── */

export function adaptImagePromptForYoutube(
  prompt: string,
  format: VideoFormat = "long"
): string {
  if (format === "short") {
    // 숏폼: 정사각(1:1) 인포그래픽 슬라이드
    return prompt
      .replace(/suitable for blog reading on mobile/gi, "suitable for a full-screen 1:1 square YouTube Shorts slide")
      .replace(/suitable for a mobile feed thumbnail/gi, "suitable for a 1:1 square YouTube Shorts slide")
      .replace(/16:9 1280x720 aspect ratio/gi, "1:1 1080x1080 square aspect ratio")
      .replace(/16:9 1920x1080 aspect ratio/gi, "1:1 1080x1080 square aspect ratio")
      .replace(/16:9 aspect ratio/gi, "1:1 1080x1080 square aspect ratio")
      .trim();
  }
  return prompt
    .replace(/suitable for blog reading on mobile/gi, "suitable for a full-screen YouTube explainer video slide")
    .replace(/suitable for a mobile feed thumbnail/gi, "suitable for a full-screen YouTube video slide")
    .replace(/16:9 1280x720 aspect ratio/gi, "16:9 1920x1080 aspect ratio")
    .replace(/16:9 aspect ratio/gi, "16:9 1920x1080 aspect ratio")
    .trim();
}
